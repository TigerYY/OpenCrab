# OpenCrab 标准部署模板（Phase 3 E1）
# 启动顺序：postgres -> redis -> control-plane -> web-console
# 使用：docker compose up -d && docker compose ps
# 注：Compose V2 不再使用 version 字段，已移除以避免警告

services:
  postgres:
    image: postgres:16
    container_name: opencrab-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: opencrab
      POSTGRES_PASSWORD: opencrab
      POSTGRES_DB: opencrab
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U opencrab -d opencrab"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  redis:
    image: redis:7
    container_name: opencrab-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  control-plane:
    build:
      context: ./apps/control-plane
      dockerfile: Dockerfile
    container_name: opencrab-control-plane
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://opencrab:opencrab@postgres:5432/opencrab
      REDIS_URL: redis://redis:6379
    ports:
      - "3000:3000"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3000/api/health"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  web-console:
    build:
      context: ./apps/web-console
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:3000/api
    container_name: opencrab-web-console
    restart: unless-stopped
    depends_on:
      control-plane:
        condition: service_healthy
    ports:
      - "8080:80"

volumes:
  pg_data:
  redis_data:
